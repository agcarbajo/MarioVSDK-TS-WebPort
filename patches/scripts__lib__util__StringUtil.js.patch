diff --git a/scripts/lib/util/StringUtil.js b/scripts/lib/util/StringUtil.js
index 72826e9..9a3958d 100644
--- a/scripts/lib/util/StringUtil.js
+++ b/scripts/lib/util/StringUtil.js
@@ -87,13 +87,17 @@ var lib;
         }
         util.lineBreak = lineBreak;
 
-        function clampComment(str, limit) {
-            if (typeof limit === "undefined") { limit = 99; }
-            if (str.length > limit) {
-                str = str.substring(0, limit) + String.fromCharCode(0x2026);
-            }
-            return str;
-        }
+        function clampComment(str, limit) {
+            if (typeof limit === "undefined") { limit = 99; }
+            if (str === null || str === undefined) {
+                return "";
+            }
+            str = String(str);
+            if (str.length > limit) {
+                str = str.substring(0, limit) + String.fromCharCode(0x2026);
+            }
+            return str;
+        }
         util.clampComment = clampComment;
 
         (function (StringUtil) {
