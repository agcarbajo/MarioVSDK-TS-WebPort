diff --git a/scripts/ui/game/HUDBar.js b/scripts/ui/game/HUDBar.js
index b73213b..c3a0475 100644
--- a/scripts/ui/game/HUDBar.js
+++ b/scripts/ui/game/HUDBar.js
@@ -264,11 +264,15 @@ var ui;
                 this._hudElementToUpdate = game.HUDElement.Count;
             };
 
-            HUDBar.prototype.render = function (ctx) {
-                ctx.identity();
-
-                ctx.drawImage(this._composite, 0, 0);
-            };
+            HUDBar.prototype.render = function (ctx) {
+                ctx.save();
+                try {
+                    ctx.identity();
+                    ctx.drawImage(this._composite, 0, 0);
+                } finally {
+                    ctx.restore();
+                }
+            };
 
             HUDBar.prototype.setHUDElementUpdate = function (element, pulse) {
                 if (this._hudElementToUpdate === element && this._isPulsing === pulse)
