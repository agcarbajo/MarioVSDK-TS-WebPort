diff --git a/scripts/pt/map/tile/GroundTile.js b/scripts/pt/map/tile/GroundTile.js
index b78396c..9cd2f50 100644
--- a/scripts/pt/map/tile/GroundTile.js
+++ b/scripts/pt/map/tile/GroundTile.js
@@ -98,11 +98,14 @@ var pt;
                     configurable: true
                 });
 
-                GroundTile.prototype.render = function (ctx) {
-                    if (!this.visible || this._renderType === 0)
-                        return;
-
-                    var rType = this._renderType;
+                GroundTile.prototype.render = function (ctx) {
+                    if (!this.visible || this._renderType === 0)
+                        return;
+
+                    var rootCtx = ctx;
+                    rootCtx.save();
+                    try {
+                    var rType = this._renderType;
 
                     ctx.imageSmoothingEnabled = this.imageSmoothingEnabled;
 
@@ -321,12 +324,15 @@ var pt;
                             ctx.drawImageQuad(this.decoration.capNE, x + this.width / 2, 0);
                     }
 
-                    if (hasCache && overlayIQ) {
-                        origCTX.identity();
-
-                        origCTX.drawImageQuad(overlayIQ, this._calcX + this.offset.x - this.width / 2, this._calcY + this.offset.y);
-                    }
-                };
+                    if (hasCache && overlayIQ) {
+                        origCTX.identity();
+
+                        origCTX.drawImageQuad(overlayIQ, this._calcX + this.offset.x - this.width / 2, this._calcY + this.offset.y);
+                    }
+                    } finally {
+                        rootCtx.restore();
+                    }
+                };
 
                 GroundTile.prototype.getVariant = function (n, ne, e, se, s, sw, w, nw) {
                     var TT = tile.TileType;
