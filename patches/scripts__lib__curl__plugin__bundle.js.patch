diff --git a/scripts/lib/curl/plugin/bundle.js b/scripts/lib/curl/plugin/bundle.js
index 904f8fb..bba4718 100644
--- a/scripts/lib/curl/plugin/bundle.js
+++ b/scripts/lib/curl/plugin/bundle.js
@@ -26,13 +26,9 @@ define(/*=='curl/plugin/bundle',==*/ function () {
 	return {
 
 		load: function (absId, require, loaded, config) {
-			var file,
-				errback  = loaded['error'] || error,
+			var errback  = loaded['error'] || error,
 				option   = absId.split('!'),
-				relPath  = require['toUrl'](option[0]).replace('file:///vol/content/app/',''),
-				io       = global.nwf.io,
-				IOEvent  = global.nwf.events.IOEvent,
-				isLoaded = false; // state
+				relPath  = require['toUrl'](option[0]).replace('file:///vol/content/app/','');
 
 			// set options
 			option = ( option.length > 1 ) ? option[option.length-1].toLowerCase() : Options.NONE;
@@ -46,53 +42,24 @@ define(/*=='curl/plugin/bundle',==*/ function () {
 			}
 		/* #endif */
 
-			file = new io.File( relPath );
-
-			if( !file.exists ) onError( {} );
-
-			// add events
-			file.addEventListener( IOEvent.READ_COMPLETE, onLoad,  this );
-			file.addEventListener( IOEvent.ERROR, 		  onError, this );
-
 		/* #ifdef PROFILE_ENABLED */
 			if( PRINT_STATS ) vsm.stats.startTimeCapture( absId + ' dataLoad' );
 		/* #endif */
 
-			// kick off load
-			file.readAsTextureBundle({
-				encoding: option,
-				type: 'image/vnd.nintendo.gtx'
-			});
-
-			function onLoad ( evt ) {
-				if( isLoaded ) return; // don't load twice
-
-				isLoaded = true;
-
-			/* #ifdef PROFILE_ENABLED */
-				if( PRINT_STATS ) {
-					vsm.stats.stopTimeCapture( absId + ' dataLoad' );
-				}
-			/* #endif */
-
-				if( !evt.data ) onError( {} );
-
-				// clean-up
-				file.removeAllEventListeners();
-				file = null;
-
-				loaded( evt.data );
+			if( !global.ChromiumPort || !global.ChromiumPort.loadGtxBundle ) {
+				return onError({ errorID: 1, message: "ChromiumPort.loadGtxBundle is not available" });
+			}
 
-			/* #ifdef PROFILE_ENABLED */
+			global.ChromiumPort.loadGtxBundle(relPath).then(function (images) {
+				if( PRINT_STATS ) vsm.stats.stopTimeCapture( absId + ' dataLoad' );
+				loaded(images);
 				if( PRINT_STATS ) vsm.stats.stopTimeCapture( absId + ' totalLoad' );
-			/* #endif */
-			}
+			}, onError);
 
 			function onError ( evt ){
-				//console.log( evt );
 				var ex = new global.Error();
-				ex.errorID = evt.errorID;
-				ex.message = "bundle! load error: " + absId + " ( errorID: " + evt.errorID + " )";
+				ex.errorID = evt && evt.errorID;
+				ex.message = "bundle! load error: " + absId + " ( " + ((evt && evt.message) || "unknown error") + " )";
 				errback( ex );
 			}
 
@@ -106,4 +73,4 @@ define(/*=='curl/plugin/bundle',==*/ function () {
 	};
 
 });
-}( this ));
\ No newline at end of file
+}( this ));
